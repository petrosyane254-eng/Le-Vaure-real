"use client";import{useEffect,useState}from"react";import{useParams}from"next/navigation";import ProductCard from "@/components/ProductCard";
export default function Collection(){const{slug}=useParams<{slug:string}>();const[d,setD]=useState<any>(null);useEffect(()=>{fetch(`/backend-api/collections/${slug}/`,{cache:"no-store"}).then(r=>r.json()).then(setD)},[slug]);if(!d)return <div className="empty container">Loading…</div>;return <main><div className="page-hero"><div className="container"><div className="eyebrow">COLLECTION</div><h1>{d.collection.name}</h1><p>{d.collection.description}</p></div></div><section className="section"><div className="container"><div className="grid">{d.products.map((p:any)=><ProductCard key={p.id} p={p}/>)}</div></div></section></main>}
