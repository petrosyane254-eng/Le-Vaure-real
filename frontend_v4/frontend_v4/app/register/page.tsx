"use client";import{useState}from"react";import{useRouter}from"next/navigation";import{apiFetch}from"@/lib/api";
export default function Register(){const r=useRouter();const[f,setF]=useState({firstName:"",lastName:"",email:"",password:""});const[error,setError]=useState("");async function submit(e:any){e.preventDefault();try{await apiFetch("/auth/register/",{method:"POST",body:JSON.stringify(f)});sessionStorage.setItem("verify_email",f.email);r.push("/verify-email")}catch(x:any){setError(x.message)}}return <main className="auth-shell"><form className="auth-card" onSubmit={submit}><div className="eyebrow">LE VAURÉ ACCOUNT</div><h1>Create account</h1>{error&&<p className="error">{error}</p>}{[["firstName","FIRST NAME"],["lastName","LAST NAME"],["email","EMAIL"],["password","PASSWORD"]].map(([k,l])=><div className="field" key={k}><label>{l}</label><input type={k==="password"?"password":k==="email"?"email":"text"} value={(f as any)[k]} onChange={e=>setF({...f,[k]:e.target.value})} required/></div>)}<button className="btn" style={{width:"100%"}}>CREATE ACCOUNT</button></form></main>}
