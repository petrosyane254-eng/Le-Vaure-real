"use client";import Link from"next/link";import{useEffect,useState}from"react";
export default function Journal(){const[d,setD]=useState<any[]>([]);useEffect(()=>{fetch("/backend-api/journal/",{cache:"no-store"}).then(r=>r.json()).then(x=>setD(x.results||[]))},[]);return <main><div className="page-hero"><div className="container"><div className="eyebrow">STORIES / CULTURE / DESIGN</div><h1>JOURNAL</h1></div></div><section className="section"><div className="container"><div className="journal-grid">{d.map(j=><Link className="journal-card" href={`/journal/${j.slug}`} key={j.slug}><img src={j.coverImage} alt={j.title}/><h3>{j.title}</h3><p>{j.excerpt}</p></Link>)}</div></div></section></main>}

