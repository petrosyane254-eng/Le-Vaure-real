"use client";import{useSearchParams}from"next/navigation";import{Suspense,useState}from"react";import{apiFetch}from"@/lib/api";import Link from"next/link";
function SuccessContent(){const q=useSearchParams();const id=q.get("id");const num=q.get("number");const[code,setCode]=useState("");const[msg,setMsg]=useState("");async function verify(e:any){e.preventDefault();try{await apiFetch(`/orders/${id}/verify/`,{method:"POST",body:JSON.stringify({code})});setMsg("Order confirmed successfully.")}catch(x:any){setMsg(x.message)}}return <main className="auth-shell"><div className="auth-card"><div className="eyebrow">ORDER RECEIVED</div><h1>Thank you.</h1><p>Your order <strong>{num}</strong> was created. We sent a 6-digit confirmation code to your email.</p><form onSubmit={verify}><div className="field"><label>ORDER CONFIRMATION CODE</label><input maxLength={6} value={code} onChange={e=>setCode(e.target.value.replace(/\D/g,""))}/></div><button className="btn" style={{width:"100%"}}>CONFIRM ORDER</button></form>{msg&&<p className="success">{msg}</p>}<p><Link href="/account">View my account →</Link></p></div></main>}

export default function Success() { return <Suspense fallback={<p role="status">Loading…</p>}><SuccessContent /></Suspense>; }
