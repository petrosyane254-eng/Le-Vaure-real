"use client";import Link from"next/link";import{useSearchParams,useRouter}from"next/navigation";import{Suspense,useState}from"react";import{apiFetch}from"@/lib/api";
function LoginContent(){const r=useRouter();const q=useSearchParams();const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[error,setError]=useState("");async function submit(e:any){e.preventDefault();setError("");try{await apiFetch("/auth/login/",{method:"POST",body:JSON.stringify({email,password})});r.push(q.get("next")||"/account");r.refresh()}catch(x:any){setError(x.message)}}return <main className="auth-shell"><form className="auth-card" onSubmit={submit}><div className="eyebrow">WELCOME BACK</div><h1>Sign in</h1>{error&&<p className="error">{error}</p>}<div className="field"><label>EMAIL</label><input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></div><div className="field"><label>PASSWORD</label><input type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></div><button className="btn" style={{width:"100%"}}>SIGN IN</button><p><Link href="/forgot-password">Forgot password?</Link></p><p>New here? <Link href="/register">Create account</Link></p></form></main>}

export default function Login() { return <Suspense fallback={<p role="status">Loading…</p>}><LoginContent /></Suspense>; }
