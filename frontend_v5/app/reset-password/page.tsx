"use client";import{useEffect,useState}from"react";import{useRouter}from"next/navigation";import{apiFetch}from"@/lib/api";
export default function Reset(){const r=useRouter();const[email,setEmail]=useState("");const[code,setCode]=useState("");const[password,setPassword]=useState("");const[msg,setMsg]=useState("");useEffect(()=>setEmail(sessionStorage.getItem("reset_email")||""),[]);async function submit(e:any){e.preventDefault();try{await apiFetch("/auth/reset-password/",{method:"POST",body:JSON.stringify({email,code,password})});r.push("/login")}catch(x:any){setMsg(x.message)}}return <main className="auth-shell"><form className="auth-card" onSubmit={submit}><h1>Choose new password</h1>{msg&&<p className="error">{msg}</p>}<div className="field"><label>EMAIL</label><input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></div><div className="field"><label>RESET CODE</label><input value={code} onChange={e=>setCode(e.target.value)} required/></div><div className="field"><label>NEW PASSWORD</label><input type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></div><button className="btn" style={{width:"100%"}}>UPDATE PASSWORD</button></form></main>}
