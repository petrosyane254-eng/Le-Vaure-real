"use client";import{useState}from"react";import{apiFetch}from"@/lib/api";import Link from"next/link";
export default function Forgot(){const[email,setEmail]=useState("");const[msg,setMsg]=useState("");async function submit(e:any){e.preventDefault();try{const d:any=await apiFetch("/auth/forgot-password/",{method:"POST",body:JSON.stringify({email})});sessionStorage.setItem("reset_email",email);setMsg(d.message)}catch(x:any){setMsg(x.message)}}return <main className="auth-shell"><form className="auth-card" onSubmit={submit}><div className="eyebrow">ACCOUNT RECOVERY</div><h1>Reset password</h1><div className="field"><label>EMAIL</label><input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></div><button className="btn" style={{width:"100%"}}>SEND RESET CODE</button>{msg&&<p className="success">{msg}</p>}<p><Link href="/reset-password">I already have a code →</Link></p></form></main>}
