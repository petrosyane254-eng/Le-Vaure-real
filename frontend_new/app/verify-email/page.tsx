"use client";import{useState}from"react";import{useRouter}from"next/navigation";import{apiFetch}from"@/lib/api";
export default function Verify(){const r=useRouter();const[code,setCode]=useState("");const[error,setError]=useState("");async function submit(e:any){e.preventDefault();try{await apiFetch("/auth/verify-email/",{method:"POST",body:JSON.stringify({code})});r.push("/account")}catch(x:any){setError(x.message)}}return <main className="auth-shell"><form className="auth-card" onSubmit={submit}><div className="eyebrow">EMAIL VERIFICATION</div><h1>Check your inbox</h1><p>Enter the 6-digit code sent to your email.</p>{error&&<p className="error">{error}</p>}<div className="field"><label>VERIFICATION CODE</label><input inputMode="numeric" maxLength={6} value={code} onChange={e=>setCode(e.target.value.replace(/\D/g,""))} required/></div><button className="btn" style={{width:"100%"}}>VERIFY EMAIL</button></form></main>}
